- name: Run general docker compose
  community.docker.docker_compose_v2:
    pull: always
    project_src: "{{ docker_compose_path }}"
    assume_yes: true
    state: present
    build: always
    recreate: "{{ 'always' if docker_compose_force_recreate else 'auto' }}"
    remove_orphans: true
